{
  "name": "Python Turtle (Codespaces)",
  "image": "mcr.microsoft.com/devcontainers/python:3.11",
  "features": {
    "ghcr.io/devcontainers/features/desktop-lite:1": {
      "webPort": "6080",
      "vncPort": "5901",
      "password": "vscode"
    }
  },
  "forwardPorts": [6080, 5901],
  "customizations": {
    "vscode": {
      "extensions": [
        "ms-python.python",
        "ms-vsliveshare.vsliveshare"
      ],
      "settings": {
        "editor.fontSize": 16,
        "python.defaultInterpreterPath": "/usr/local/bin/python",
        "chat.disableAIFeatures": true,

        "editor.inlineSuggest.enabled": true,
        "editor.suggest.showInlineDetails": true,
        "editor.quickSuggestions": {
          "other": true,
          "comments": false,
          "strings": false
        },
      }
    }
  },
  "postCreateCommand": "sudo apt-get update -y && sudo apt-get install -y python3-tk && pip install --upgrade pip && if [ -f requirements.txt ]; then pip install -r requirements.txt; fi && python -m ipykernel install --user --name devcontainer_kernel --display-name \"Python (Dev Container)\""
}